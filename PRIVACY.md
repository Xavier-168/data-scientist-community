# 隐私说明

版本：v0.1.0 发布候选

本社区版采用本地优先设计。默认运行不配置许可证、更新、反馈或遥测服务，但采集功能本身必须访问用户选择的平台；飞书同步和自建服务由用户明确配置或触发。

## 数据存储

已封装桌面应用和直接源码运行的默认状态均位于 `~/Library/Application Support/数据科学家 Community/`；封装包清单包含 `package_id` 时，还会使用对应子目录。源码目录不保存运行状态；只有用户在启动前显式设置 `YIRENGONGIS_STATE_DIR` 时才使用其他目录。状态可能包括：

- 浏览器授权 Profile、Cookie 和授权状态；
- 平台作品数据、JSON、CSV、Excel 与导出缓存；
- SQLite 分析库、运行历史、同步 checkpoint 和日志；
- 飞书 App ID、App Secret、App Token、表 ID 等本地配置；
- 用户设置的客户/工作区显示名、日期和平台范围。
- 飞书 CLI 的隔离 HOME，以及权限为仅当前用户可读写的临时请求文件；临时文件在请求结束后删除，超过 24 小时的残留文件会在下次请求时清理。

这些内容不应进入 Git、Issue、构建产物或公开附件。删除应用不会必然删除状态目录；用户可在退出应用后手动删除相应目录。删除前应备份自己需要的 Excel。

## 外部网络行为

| 目的地 | 触发条件 | 发送字段 | 设备/个人信息 | 必要性 | 默认启用 | 用户知情方式 | 社区版处理 |
|---|---|---|---|---|---:|---|---|
| `127.0.0.1` 本地服务 | 启动和页面操作 | 本地 API 请求、配置与采集结果 | 仅本机进程间传输 | 核心功能 | 是 | README 与本地 UI | 仅绑定回环地址 |
| 抖音创作者后台 | 用户选择抖音授权/采集 | 浏览器请求、登录态 Cookie、作品查询条件 | 账号和作品数据由平台接收 | 抖音采集必需 | 否 | 可见浏览器授权与平台选择 | 浏览器直连；结果写本机 |
| 小红书创作者后台 | 用户选择小红书授权/采集 | 浏览器请求、登录态 Cookie、作品查询条件 | 同上 | 小红书采集必需 | 否 | 同上 | 浏览器直连；结果写本机 |
| B站站点、创作中心与公开 API | 用户选择 B站授权/采集 | 浏览器请求、Cookie、稿件查询、日期和分页参数 | 同上 | B站采集必需 | 否 | 同上 | 浏览器直连；结果写本机 |
| 快手创作者平台 | 用户选择快手授权/采集 | 浏览器请求、Cookie、作品查询参数 | 同上 | 快手采集必需 | 否 | 同上 | 浏览器直连；结果写本机 |
| 飞书开放平台 | 用户配置凭据并测试、创建或同步 | App ID/App Secret 换取 Token；多维表格结构、作品字段、记录 ID | 作品数据、工作区/表信息；凭据 | 仅飞书功能必需 | 否 | 设置页开关、连接测试和同步按钮 | 用户自己的飞书租户；可关闭 |
| 用户自建许可证服务 | 用户显式设置服务地址并激活/验证 | 许可证密钥、哈希机器 ID、系统/架构/hostname、包和平台信息 | 是 | 社区核心不需要 | 否 | `.env.example` 和此表 | 默认地址为空；社区访问不依赖许可证；免费试用已关闭 |
| 用户自建更新服务 | 用户显式设置服务地址；页面启动后静默检查或手动检查 | package ID、平台、架构、当前版本 | 通常不含直接身份信息 | 社区核心不需要 | 否 | 设置和更新 UI | 默认地址为空；不自动外发 |
| 用户自建反馈服务 | 用户填写反馈并点击发送 | 反馈正文、客户/工作区显示名、页面路径、User-Agent、包信息、时间 | 可能包含用户主动输入的信息 | 非必需 | 否 | 发送按钮和表单 | 社区默认地址为空。若用户配置了自建激活服务而未单独配置反馈地址，当前实现会尝试 `<activation-server>/feedback/send`；只在用户主动提交反馈时发送 |
| npm、PyPI、Playwright/CDN、crates.io | 用户安装依赖或浏览器 | 包名、版本、IP 及包管理器常规请求信息 | 由供应商处理 | 安装/构建需要 | 否 | 安装命令 | 不属于应用运行遥测 |
| npm 与飞书登录（Lark CLI 模式） | 用户选择 CLI 授权 | CLI 包请求、OAuth/用户授权和多维表格操作 | 飞书账号授权信息 | 仅 CLI 同步需要 | 否 | 可见授权流程 | 用户可不用 CLI 模式 |

## 不会默认做的事

- 不默认连接商业激活、更新或反馈基础设施；如用户自行配置激活服务，反馈派生规则以上表为准。
- 不托管浏览器 Cookie 或平台代理登录。
- 不把运行数据、账号信息或使用统计发送给项目维护者。
- 不在社区仓库中携带客户台账、许可证、浏览器 Profile、日志或历史安装包。

## 用户责任与最小化建议

- 仅采集自己或已获授权管理的账号和数据。
- 飞书只同步业务所需字段，并为 App 设置最小权限。
- 不在反馈、Issue 或日志中粘贴密钥、Cookie、手机号、客户名称和完整作品明细。
- 定期清理旧 Profile、导出和日志；停止使用后撤销平台与飞书授权。

## 数据请求与联系

社区版数据默认在用户设备或用户选择的第三方服务中，维护者通常无法代替用户访问、导出或删除。项目维护与公开联系主体为杭州玄野科技有限公司；当前可通过 GitHub Issues 或 Discussions 提交不含个人数据的隐私问题。若维护者未来提供托管、遥测、在线反馈或其他会接收个人信息的服务，应在启用前另行公布适用的数据控制者信息、联系渠道、保留期限和司法辖区。

本文件不是法律意见；组织部署前应依据实际地区、角色和数据流完成法律审查。
