# 安全政策

## 支持范围

当前安全维护目标是最新 `main` 与最近的社区发布候选。历史快照、第三方修改包、非官方二进制和商业私有模块不在本仓库支持范围。

## 报告漏洞

优先使用 GitHub Private Vulnerability Reporting。若仓库尚未启用该功能，请只创建一个不含技术细节、密钥、Cookie、真实数据或利用代码的 Issue，请求维护者建立私密沟通渠道。

报告建议包括：受影响版本、前置条件、最小复现、实际影响、修复建议和是否已公开。使用合成数据；不要测试不属于你的账号、设备或服务。

## 处理目标

社区维护不承诺 SLA。维护者会尽力确认接收、评估影响、准备修复、回归测试并在适当时间发布安全公告。未经协调不要公开零日细节。

## 安全边界

- 本地 HTTP 服务必须绑定回环地址，并使用现有 session/origin 保护。
- Cookie、浏览器 Profile、飞书 Secret、许可证和客户数据必须只留在运行目录，不得进入日志、Issue 或提交。
- 更新包必须校验 HTTPS、包身份、架构、大小和 SHA-256；社区版默认不配置更新服务。
- 公钥可公开；私钥、管理员密钥和签名运维脚本不属于社区仓库。
- 不接受绕过验证码、平台访问控制、账号封禁、付费墙或频率限制的“安全修复”。

依赖漏洞应同时说明上游公告、锁定版本、可利用路径和回归验证，避免只依据版本号制造误报。
