# Changelog

本项目采用简化的 Keep a Changelog 结构。社区版本从 `0.1.0` 开始，不沿用商业安装包版本号或原仓库历史。

## [Unreleased]

### Fixed

- **跨平台作品合并**：Excel 与飞书使用的标题相似度阈值统一从 `0.70` 调整为 `0.40`，使“完整标题 + 教程名”和仅含同一教程名的抖音、小红书、B站作品归入同一作品组。
- **B站一直显示准备启动**：四平台并发采集现在明确区分“排队等待”和“采集进行中”；采集器已写入 `completed`/`failed` 后，监督器给予 5 秒清理时间并结束仍滞留的浏览器/调试器进程组，及时释放下一平台的采集槽位，同时清除继承的 Node 调试环境变量。
- **原生客户端 Excel/飞书跳转**：Tauri 兼容控制台拦截新窗口，仅将飞书/Lark HTTPS 授权页和本机 `/download-excel` 下载交给系统浏览器打开，恢复飞书授权跳转与 Excel 文件下载，其他新窗口继续拒绝。

### Planned

- 完成四平台真实账号人工验收。
- 经律师审阅后启用正式 CLA 签署与留痕流程。

## [0.1.0-rc.2] - 2026-08-18

### Added

- 按 Figma 设计基线重构的本地仪表盘、平台表格、运行日志和响应式缩放约束。
- macOS 原生 Excel 保存位置选择、范围选择、XLSX 校验和原子写入链路。
- 非破坏式登录态健康监控、最近检查状态和保守的 6 小时检查周期。
- Noto Serif SC 变量字体及其 OFL 1.1；平台 SVG 权利与来源识别边界同步写入 notices 和商标声明。

### Fixed

- **跨平台作品合并**：Excel 与飞书标题相似度阈值统一为 `0.40`，同一教程的长短标题可归入同一作品组。
- **B站官方数据链**：按日期锁定稿件后真实滚动确认，并用有效的官方批次导出、字段校验和覆盖率校验替代 XHR 成功后的提前返回；进度只在文件下载完成后单调增长。
- **抖音长任务稳定性**：增加列表慢响应等待、分页游标保护、导航瞬态重试、退避和熔断；部分成功保留产物与真实进度，不再整平台重跑后覆盖为 `0/0`。
- **小红书/快手完整性**：强化页面滚动与接口分页双重确认；快手绑定本次导出任务并改进标题读取。
- **授权与调度状态**：授权、排队、采集和登录态健康状态分离；滞留浏览器进程会在确定终态后回收并释放采集槽位。
- **Excel 客户端导出**：移除依赖 JS `prompt`/外部浏览器下载的断裂路径，保存取消、无数据、无效范围和写入错误均明确反馈。
- **飞书首次初始化**：固定版本 CLI 首次准备显示真实阶段；可信验证页自动打开，同时保留手动入口；阻塞 stdout 读取改为有截止时间的监督读取。
- **前端布局**：修复画布上下灰色留白、缩放错位、进度条/数值不齐和表格底部空间失衡。

### Security

- 社区版继续关闭试用，商业激活、更新和反馈地址默认空；本地服务仅监听 `127.0.0.1`。
- 飞书自动打开仅允许 `https://open.feishu.cn/page/cli`；其他协议、域名和路径不放行。
- 公开白名单、provenance、敏感扫描、第三方 notices 和 release gate 随本次源码刷新。

## [0.1.0-rc.1] - 2026-08-13

### Added

- 从当前源码快照白名单导出的全新社区仓库历史。
- 抖音、小红书、B站、快手四个平台入口。
- 本地 Excel、飞书基础同步、分析和仪表盘源码。
- AGPL-3.0-only、商业许可说明、CLA、隐私、安全、平台合规和第三方声明。
- 公开导出清单、脱敏扫描器、纯 Python staging 构建和 SPDX SBOM。

### Changed

- 确认杭州玄野科技有限公司为项目方及公开联系主体。
- 社区版默认不连接商业许可证、更新或反馈服务。
- 关闭 14 天免费试用流程；社区源码模式继续无需许可证。
- 字体改用系统 fallback，排除来源版本无法证明的本地字体二进制。
- 使用原创社区占位图标替换权属未证明的原图标。
- 将 `cryptography` 升级至 50.0.0，并刷新受安全公告影响的 npm 间接依赖锁定版本。

### Removed

- 原 Git 历史、内部 Agent 文档、客户包生成器、发货台账、签名运维工具和商业发布管线。
- 客户数据、浏览器 Profile、日志、历史 DMG、runtime、node_modules 和缓存。
- Human State Intelligence 私有算法、数据和商业材料。
